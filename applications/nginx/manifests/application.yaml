apiVersion: core.oam.dev/v1beta1
kind: Application
metadata:
  name: nginx
spec:
  components:
    - name: nginx
      type: edgefarm-applications
      properties:
        image: nginx
        nodepoolSelector:
          matchLabels:
            ac100a: ""
        name: nginx
        cpu: 
        memory: 
      traits:
        []
        